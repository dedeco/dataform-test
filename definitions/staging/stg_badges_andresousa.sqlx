config {
    type: "view",
    schema: "staging_andresousa",
    description:"staging badges"
}

SELECT 
    id as bagde_id,
    name as badge_name,
    date as award_timestamp,
    user_id
FROM
    ${ref("badges")}