config {
    type:"view",
    schema: "staging_andresousa",
    description:"Versão limpa das respostas"
}

select
  id as post_id,
  creation_date as created_at,
  'answer' as type,
  title,
  body,
  owner_user_id,
  cast(parent_id as string) as parent_id
from
  ${ref("posts_answers")}
where
-- Limitar por data
  creation_date >= timestamp("2020-01-01")